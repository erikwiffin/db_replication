{% extends "base.jinja2" %}
{% import "main/includes/cards.jinja2" as cards %}

{% block body %}
<div class="row">
  <div class="col">
    <div class="jumbotron">
      <h2>{{ user.username }} - Friends of Friends</h2>
      <hr />
      {% if user in current_user.following %}
        <button class="btn btn-outline-secondary" type="button">✓ following</button>
      {% else %}
        <button class="btn btn-primary" type="button">follow</button>
      {% endif %}
    </div>
  </div>
</div>
<div class="row">
  <div class="col">
    <ul>
      {% for path in fof %}
        <li>
            {% for person in path %}
                <a href="/user/{{ person.username }}">{{ person.username }}</a>
                {% if not loop.last %}
                    ->
                {% endif %}
            {% endfor %}
        </li>
      {% endfor %}
    </ul>
  </div>
</div>
{% endblock %}
